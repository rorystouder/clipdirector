import { createLogger } from '@clipdirector/logger';

const log = createLogger('api-gateway');

// Phase 2: Express app, JWT middleware, POST /jobs, GET /jobs/:id, GET /jobs/:id/download, GET /health.
// See PRD Section 6.
log.info('api-gateway scaffold ready — implementation pending Phase 2');
