import { createLogger } from '@clipdirector/logger';

const log = createLogger('orchestrator');

// Phase 3: BullMQ worker, frame sampling, Whisper transcription, Claude API call, manifest validation.
// See PRD Section 7.
log.info('orchestrator scaffold ready — implementation pending Phase 3');
